[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
qt_api = pyqt5

markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    screenshot: marks tests that generate screenshots
    integration: marks integration tests
